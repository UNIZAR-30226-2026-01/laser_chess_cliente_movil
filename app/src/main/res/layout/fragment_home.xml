<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingHorizontal="24dp"
    android:paddingTop="16dp"
    android:paddingBottom="16dp">

    <include
        android:id="@+id/includeRankBadge"
        layout="@layout/view_rank_badge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <FrameLayout
        android:id="@+id/boardContainer"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="8dp"
        android:background="#2A2538"
        app:layout_constraintDimensionRatio="4:5"
        app:layout_constraintTop_toBottomOf="@id/includeRankBadge"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/layoutPlayButtons">

        <ImageView
            android:id="@+id/imgBoardPlaceholder"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="fitCenter"
            android:contentDescription="Board"/>

    </FrameLayout>

    <LinearLayout
        android:id="@+id/layoutPlayButtons"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        app:layout_constraintTop_toBottomOf="@id/boardContainer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <Button
            android:id="@+id/btnPlay"
            android:layout_width="0dp"
            android:layout_height="64dp"
            android:layout_weight="1"
            android:layout_marginEnd="12dp"
            android:backgroundTint="@color/LCPurple"
            android:text="JUGAR"
            android:textColor="@color/LCWhite"
            android:textSize="@dimen/text_large"
            android:textStyle="bold"
            app:cornerRadius="12dp"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:insetLeft="0dp"
            android:insetRight="0dp"/>

        <ImageButton
            android:id="@+id/btnGameMode"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:background="@drawable/bg_dark_panel"
            android:src="@drawable/robot_2_48px"
            app:tint="@color/LCWhite"
            android:contentDescription="modo_juego"/>
    </LinearLayout>

    <include
        android:id="@+id/includeBoardSelector"
        layout="@layout/view_selector_desplegable"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/layoutPlayButtons"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <include
        android:id="@+id/includeTimeSelector"
        layout="@layout/view_selector_desplegable"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/includeBoardSelector"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <!-- POPUP FLOTANTE MODOS -->
    <LinearLayout
        android:id="@+id/layoutGamePopup"
        android:layout_width="64dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:visibility="gone"
        android:elevation="12dp"
        app:layout_constraintBottom_toTopOf="@id/layoutPlayButtons"
        app:layout_constraintEnd_toEndOf="@id/layoutPlayButtons">

        <ImageButton
            android:id="@+id/btnModeTop"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_marginBottom="8dp"
            android:background="@drawable/bg_dark_panel"
            app:tint="@color/LCWhite"/>

        <ImageButton
            android:id="@+id/btnModeMiddle"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:background="@drawable/bg_dark_panel"
            app:tint="@color/LCWhite"/>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>